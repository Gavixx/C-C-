#include <stdio.h>
#include <Windows.h>

int main()
{
    SetConsoleCP(1251);
    SetConsoleOutputCP(1251);
    char s[80];
    int c= 0;

    printf("Введіть рядок: ");
    gets(s);

    for (int i = 0; i < 80; i++)
        if s[i] >= '0' && s[i] <= '9')
            c++;
        else if (s[i] == ' ')
            s[i] = '_';

    printf("в рядку %s цифр %d\n", str, c);
    return 0;
}