#include <stdio.h>

int main ()
{
    int n = 0;
    unsigned int factorial = 1;
    printf("input n: ");
    scanf("%d", &n);
    int i = 1;
    while(i <= n)
    {
        factorial *= i++;
    }

    printf("Факторіал(%d) = %d\n", n, factorial);
    return 0;
}
